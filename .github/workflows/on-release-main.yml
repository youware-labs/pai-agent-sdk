name: release-main

permissions:
  contents: write
  packages: write

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}  # Checkout the exact tag
          fetch-depth: 0  # Required for uv-dynamic-versioning to detect version from tags

      - name: Set up the environment
        uses: ./.github/actions/setup-python-env

      - name: Sync skills folder
        run: bash scripts/sync-skills.sh

      - name: Build all packages
        run: uv build --all-packages

      - name: Inspect package version
        id: inspect_package
        run: |
          version=$(uvx --with uv-dynamic-versioning hatchling version)
          echo "version=$version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Publish packages to PyPI
        run: uv publish
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_TOKEN }}

      - name: Upload dists to release
        uses: svenstaro/upload-release-action@v2
        with:
            repo_token: ${{ secrets.GITHUB_TOKEN }}
            file: dist/*
            file_glob: true
            tag: ${{ github.ref }}
            overwrite: true

      - name: Create SKILL.zip
        run: |
          cp -r paintress_cli/paintress_cli/skills/building-agents pai-agent-sdk
          zip -r SKILL.zip pai-agent-sdk

      - name: Upload SKILL.zip to release
        uses: svenstaro/upload-release-action@v2
        with:
            repo_token: ${{ secrets.GITHUB_TOKEN }}
            file: SKILL.zip
            tag: ${{ github.ref }}
            overwrite: true
